# Celery & Redis (Optionnel)
celery>=5.3.0
redis>=5.0.0
# flower>=2.0.0  # Optionnel - Interface web pour Celery

# Video Processing
ffmpeg-python>=0.2.0
moviepy==1.0.3
scenedetect[opencv]>=0.6.0
# opencv-python>=4.8.0  # Inclus avec scenedetect[opencv]
Pillow==9.5.0
yt-dlp>=2023.11.0
youtube-transcript-api>=0.6.0
groq
numpy<2.3.0

# AI Models - Required for YouTube video processing
transformers>=4.35.0
torch>=2.0.0
# torchaudio  # Optional
# torchvision  # Optional
# openai-whisper  # Optional
# faster-whisper  # Optional
# deepface  # Optional
# insightface  # Optional
# onnxruntime  # Optional

# OpenAI / LLM
openai>=1.0.0
# anthropic  # Optionnel

# AWS / Storage (Optionnel)
# boto3
# botocore

# Stripe (Optionnel)
stripe

# Utils
pydantic>=2.5.0
pydantic-settings>=2.1.0
requests>=2.31.0
aiohttp>=3.9.0
httpx>=0.25.0
python-slugify>=8.0.0

# Email
resend>=0.7.0
mailjet_rest>=1.3.4

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Monitoring (Optionnel)
# sentry-sdk[fastapi]

# Library Generator Dependencies
librosa>=0.10.0
scipy>=1.11.0
psutil>=5.9.0


